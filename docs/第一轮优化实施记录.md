# 第一轮前后端优化实施记录

启动日期：2026-09-23。基线提交：`578bab6`。状态：实施中。

依据：[第一轮问题检查](./第一轮问题检查.md)、[后端架构设计规范](./后端架构设计规范.md)。本轮保留迭代 1 业务范围，按完整用例整改；不以目录搬迁代替职责分离或故障验证。

## 分工与验收

用户指定两个 GPT-6 Astra（high）子代理分别负责后端与前端；主代理负责环境、集成检查、公共文档和分阶段提交推送。

| 范围 | 实施内容 | 验证边界 |
|---|---|---|
| 后端 | identity/auth/teaching_class 模块内分层，明确响应模型，集中审核与账号状态规则 | 既有真实 PostgreSQL/Redis HTTP 测试、响应字段白名单、OpenAPI、静态依赖检查 |
| 查询 | 列表分页、人数聚合、教师批量查询，响应映射无 I/O | 多班级查询数量及结果一致性 |
| 一致性 | 提交后的持久 Redis 补偿、轮换前检查、锁定后刷新和实时身份校验 | HTTP 故障注入、可控并发、补偿恢复和 CLI 验证 |
| 前端 | 业务 API 模块、分页与远程成员选择、认证及成功/故障反馈 | 保留 4 条真实浏览器流程，补分页及必要网络故障场景 |

延续已确认的 TDD 边界，按失败测试 → 最小修复 → 通过测试推进。故障注入、SQL 查询数量和架构依赖检查是本轮问题文档明确要求的补充；不以大量内部 mock 替代真实集成测试。

## 接口约定

- 列表使用 `page`、`page_size`、`q`，响应含 `items`、`total`、`page`、`page_size`；角色、状态过滤保留。班级详情按需返回该班成员。
- 数据库写入成功但 Redis 清理未完成时，保留业务成功状态，通过 `X-Session-Cleanup: pending` 表明持久补偿待处理。前端不得因此重复提交已完成操作。
- 账号重置后的延迟补偿只撤销旧认证版本会话，不应清理随后使用新密码建立的新会话。
- 轮换前数据库故障应允许使用原凭据重试；轮换最终成功后的网络交付失败有明确限制，不承诺完全无感恢复。

## 实际执行记录

| 阶段 | 结果 |
|---|---|
| 环境准备 | 恢复项目独立 PostgreSQL 55432 与 Redis 16379；开发、集成测试仍使用独立数据库和 Redis DB，不修改原有 5432 服务 |
| R1-01 首切片 | 测试复现注册申请响应多出 ORM 字段 `user_id`，改为 ApplicationPublic/RegistrationResponse 并补 OpenAPI；针对性 7 项通过，完整响应契约整改继续推进 |

各问题完成状态以实际实现、测试和提交为准；最终命令、结果及剩余限制在交付时补齐。本轮临时启动的 API 与前端预览在测试结束后关闭，遵循用户此前的关闭要求。
