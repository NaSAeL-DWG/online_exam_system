# 迭代 1 验证记录

日期：2026-09-20。当前状态：实施中，以下仅为验证约定，执行结果随实施补充。

## 范围与测试边界

沿用需求规格与实施计划已确认的边界：FastAPI HTTP API 配合真实 PostgreSQL、Redis；前端使用少量 Playwright 完整流程。通过公开响应验证用户可观察行为，不以 SQLite、伪造 Session 或内部函数调用次数替代集成验收。

按垂直功能切片执行失败测试 → 最小实现 → 通过测试，记录实际命令与结果。工程初始化、文档和样式无需单独编写实现镜像测试。

| 验收项 | 本次验证范围 |
|---|---|
| AC01 | 待激活账号限制、拒绝后重新申请、通过后访问、停用后旧会话失效 |
| AC02 | 教学班关联教师权限、学生多班、多人负责、归档限制；题库共享与答卷权限随迭代 2/3 实现 |
| AC25 | Cookie/CSRF、Session 校验、轮换与旧刷新凭据拒绝、Redis 故障关闭、停用和重置撤销、临时密码限制 |
| AC26 | 邮箱/手机号验证明确返回未启用，不能获得验证或找回权限 |
| 身份管理 | 统一账号唯一、审核历史、管理员创建教师/身份更正、联系方式修改验证密码、初始化及运维重置 |
| 浏览器流程 | 注册与审核、登录与强制改密、管理员教师/班级管理、角色路由和错误反馈 |

## 执行记录

实施中。完成后补充环境版本、红绿证据、最终检查及实际未验证项。
